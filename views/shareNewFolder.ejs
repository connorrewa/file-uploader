<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Share New Folder</title>
    </head>
    <body>
        <div class="mt-4">
            <h2 class="text-xl font-semibold">Share Folder</h2>
            <form action="/share-folder" method="POST">
                <input type="hidden" name="folderId" value="<%= folder.id %>" />
                <label for="duration">Share Duration (days):</label>
                <input
                    type="number"
                    id="duration"
                    name="duration"
                    min="1"
                    required
                />
                <button
                    type="submit"
                    class="text-white bg-primary-600 hover:bg-primary-700 focus:ring-4 focus:outline-none focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center"
                >
                    Share Folder
                </button>
            </form>
        </div>
    </body>
</html>
