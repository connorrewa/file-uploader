<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link href="/css/tailwind.css" rel="stylesheet" />
        <title>Share New Folder</title>
    </head>
    <body class="font-body min-h-screen flex flex-col bg-gray-50">
        <%- include('partials/navbar', { currentUser: currentUser }) %>
        <section class="flex-grow max-w-screen-xl mx-auto">
            <div class="mx-auto px-6 py-8">
                <h2 class="text-2xl font-bold text-gray-900">Share Folder</h2>
                <form
                    action="/share-folder"
                    method="POST"
                    class="mt-4 space-y-4"
                >
                    <input
                        type="hidden"
                        name="folderId"
                        value="<%= folder.id %>"
                    />
                    <div>
                        <label
                            for="duration"
                            class="block text-sm font-medium text-gray-700"
                            >Share Duration (days):</label
                        >
                        <input
                            type="number"
                            id="duration"
                            name="duration"
                            min="1"
                            required
                            class="mt-1 block w-full p-2 border border-gray-300 rounded-lg shadow-sm focus:ring-primary-600 focus:border-primary-600"
                        />
                    </div>
                    <button
                        type="submit"
                        class="w-full text-white bg-primary-600 hover:bg-primary-700 focus:ring-4 focus:outline-none focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center"
                    >
                        Create Shared Folder
                    </button>
                </form>
            </div>
        </section>
    </body>
</html>
