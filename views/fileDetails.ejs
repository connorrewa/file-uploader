<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link href="/css/tailwind.css" rel="stylesheet" />
        <script
            src="https://kit.fontawesome.com/119d85fe28.js"
            crossorigin="anonymous"
        ></script>

        <title>File Details</title>
    </head>
    <body class="font-body bg-gray-50">
        <%- include('partials/navbar') %>
        <section
            class="flex flex-col items-center px-6 py-8 mx-auto h-screen lg:py-0"
        >
            <div
                class="w-full bg-white rounded-lg shadow dark:border mt-4 sm:max-w-md xl:p-0"
            >
                <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
                    <div class="flex justify-between items-center">
                        <a href="javascript:history.back()">
                            <i class="fa-solid fa-arrow-left fa-xl"></i>
                        </a>
                        <h1
                            class="text-xl font-bold leading-tight tracking-tight text-gray-900 md:text-2xl text-right"
                        >
                            <%= file.name %>
                        </h1>
                    </div>
                    <ul class="space-y-2">
                        <li>
                            <strong>Size:</strong> <%= (file.size / 1024 /
                            1024).toFixed(2) %> MB
                        </li>
                        <li>
                            <strong>Uploaded At:</strong> <%=
                            file.createdAt.toLocaleString() %>
                        </li>
                    </ul>

                    <% if (isImage) { %>
                    <h2
                        class="text-lg font-semibold leading-tight tracking-tight text-gray-900 md:text-xl"
                    >
                        Image Preview
                    </h2>
                    <img
                        src="<%= file.url %>"
                        alt="<%= file.name %>"
                        class="max-w-full h-auto rounded-lg"
                    />
                    <% } %>

                    <form
                        action="/files/details/download/<%= file.id %>"
                        method="GET"
                        class="mt-4"
                    >
                        <button
                            type="submit"
                            class="w-full text-white bg-primary-600 hover:bg-primary-700 focus:ring-4 focus:outline-none focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center"
                        >
                            Download File
                        </button>
                    </form>
                </div>
            </div>
        </section>
    </body>
</html>
